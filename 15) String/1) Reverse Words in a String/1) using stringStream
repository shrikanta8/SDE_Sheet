class Solution {
public:
    string reverseWords(string s) {
        stringstream str(s);
        string ans="",word;
        
        while(str>>word){
            // cout<<word<<endl;
            ans = " "+ ans;
            ans = word + ans;
            
        }
        ans.pop_back();
        return ans;
    }
};
